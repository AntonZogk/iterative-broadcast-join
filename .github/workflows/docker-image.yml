name: test_docker

on:
  push:
    branches: [ "master" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: test docker
      run:  |
             git clone https://github.com/cloudera/ml-runtimes.git
             docker build -t ml-runtimes .
             docker image ls


             
