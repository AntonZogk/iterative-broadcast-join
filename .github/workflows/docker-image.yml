name: test_docker

on:
  push:
    branches: [ "master" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: test docker
      run:  |
             git clone https://github.com/cloudera/ml-runtimes.git
             cd ml-runtimes
             ls
             echo "$PWD"
             docker build ml-runtimes .
             docker image ls
